dependencies:
  flutter:
    sdk: flutter

  # Firebase (updated for compatibility)
  firebase_core: ^4.6.0
  firebase_auth: ^6.1.4
  google_sign_in: ^6.2.1

  # UI & Core
  cupertino_icons: ^1.0.6
  google_fonts: ^6.2.1

  # Gemini AI + Media Verifier
  http: ^1.2.2
  image_picker: ^1.1.2
